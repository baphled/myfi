<%= simple_form_for @transaction do |f| %>
  <%= f.input :type %>
  <%= f.input :amount %>

  <%= f.button :submit, "Income" %>
  Income: <%= @income.collect(&:amount).map(&:to_f).inject(&:+) %>
<% end %>
